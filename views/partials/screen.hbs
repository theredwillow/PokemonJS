{{#if user}}

<script>
    var mainMenu = new MenuDisplay();
    mainMenu.options = [
        "Resume Game",
        "New Game",
        "Settings"
    ];
    mainMenu.finalize = function(selection) {
        alert("You chose to " + selection);
    };
    var drawMenu = function() {
        var gameboyScreen = document.getElementById("gameboy_screen");
        mainMenu.draw(gameboyScreen);
    }
    window.addEventListener("load", drawMenu);
</script>

{{/if}}
